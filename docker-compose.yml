name: tibo-reset-radar

services:
  radar:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    env_file: .env
    environment:
      RADAR_DB_PATH: /data/radar.db
    volumes:
      - radar-data:/data
    ports:
      - "127.0.0.1:4173:8787"

volumes:
  radar-data:
